export default class UserModel {
  constructor(userName, email, password, gender, _id) {
    this.userName = userName;
    this.email = email;
    this.password = password;
    this.gender = gender;
    this._id = _id;
  }
}
